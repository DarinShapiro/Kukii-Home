# generated by datamodel-codegen:
#   filename:  frame-window.schema.json

from __future__ import annotations

from enum import StrEnum
from typing import Any

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, RootModel


class Frame(BaseModel):
    model_config = ConfigDict(
        extra="allow",
    )
    uri: str
    timestamp: AwareDatetime
    width: int | None = None
    height: int | None = None


class MotionRegion(RootModel[list[float]]):
    root: list[float] = Field(..., max_length=4, min_length=4)


class PreprocessingMode(StrEnum):
    native = "native"
    built_in = "built-in"
    service = "service"
    direct = "direct"


class Metadata(BaseModel):
    model_config = ConfigDict(
        extra="allow",
    )
    motion_regions: list[MotionRegion] | None = None
    detections: list[dict[str, Any]] | None = None
    preprocessing_mode: PreprocessingMode | None = None
    preprocessing_latency_ms: int | None = Field(None, ge=0)


class FrameWindow(BaseModel):
    """
    Result of nvr.get_frame_window — selected frames + preprocessing metadata for a time range on a single camera.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    camera_id: str
    ts_start: AwareDatetime
    ts_end: AwareDatetime
    frames: list[Frame] = Field(..., min_length=0)
    metadata: Metadata | None = None
