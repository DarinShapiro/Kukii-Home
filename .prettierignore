# Generated artifacts
**/generated/**
**/dist/**
**/build/**
**/.next/**
**/coverage/**

# Lock files
pnpm-lock.yaml
package-lock.json
yarn.lock
uv.lock

# Python / venv
.venv/
**/__pycache__/**
*.pyc

# Documentation generated content
docs/_build/

# semantic-release regenerates this changelog on EVERY release (via
# @semantic-release/changelog) without running Prettier, so format-checking
# it is perpetual whack-a-mole — the next release re-dirties it. Generated
# artifact → exempt from the format gate.
ha-integration/addon/kukiihome/CHANGELOG.md

# Vendored HA integration: shipped to /config/custom_components and
# content-hashed by the add-on installer. Formatting is frozen on
# purpose — churning these files triggers an integration re-install +
# HA restart notification. Keep them byte-stable.
ha-integration/custom_components/
